server:
  port: ${port:9001}


spring:
  application:
    name: LISTS-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/dians-proba-mapa?currentSchema=dians-proba-mapa


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: app0

